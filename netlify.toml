[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — send all routes to index.html so react-router works on page refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
